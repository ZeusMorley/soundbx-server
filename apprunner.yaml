version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - rm -rf node_modules package-lock.json
      - npm install
      - npm run build
run:
  command: npm start
  network:
    port: 3000